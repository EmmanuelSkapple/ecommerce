export interface ProductType {
    id: number;
    name: string;
    price: string;
    description: string;
}

export interface ProductFormType {
    name: string;
    price: string;
    description: string;
}